/**
 * Executed a trip
 * @param {import('../models/Trip').Trip} tripToExecute 
 * @returns {bool} true if successful else false
 */
function executeTrip(tripToExecute) { }

/**
 * Check if the transport mode is available for this route
 * @param {string} transport 
 * @returns {bool} true if valid else false
 */
function isValidTransport(transport) { }